/**
 * Sent to the client on every MCP `initialize` — always reflects
 * whatever version of this server is actually running, so it stays
 * in sync automatically as this file changes across releases. No
 * separate install/upload step, unlike the (now supplementary)
 * skills/veilwright-workflow.zip Claude Desktop skill, which the
 * user has to manually re-upload after every update to see changes.
 * The detailed widget/theme-builder/Fluent-Forms reference tables
 * are deliberately kept out of here (too long to send on every
 * session) and exposed instead as the `elementor-widgets` and
 * `fluent-forms` resources this server registers — read those on
 * demand instead.
 */
export const SERVER_INSTRUCTIONS = `Veilwright bridges you to one or more of the user's WordPress + Elementor Pro sites.

1. Resolve the domain to a site every time — call \`site_list\` and match the domain the user mentioned against each site's \`url\` (strip protocol/www, compare case-insensitively). Don't rely on whichever site a previous \`site_select\` left active: this one process is plausibly shared by other open conversations. No match → tell the user to run \`site_add\` first. Multiple plausible matches → ask which one.
2. Pass that site's \`id\` as \`siteId\` explicitly on every tool call for the task, not just once via \`site_select\`.
3. Picking a page tool: plain content with no layout described → \`page_create\`. A design/layout/HTML given or implied → \`page_createFromHtml\` (builds a real native Elementor page, not an HTML dump; it's async — returns \`{ jobId, status: "queued" }\`, poll \`job_status\` until \`"completed"\`/\`"failed"\`, don't claim done before that). A specific named widget not covered by createFromHtml → \`page_addElementorWidget\` (read the \`elementor-widgets\` resource first for the right \`widgetType\`/\`settings\` shape and how reliable it is).
4. \`page_createFromHtml\` always leaves the page as a draft even once the job completes — call \`page_update({ id, status: "publish" })\` afterwards unless the user asked to leave it as a draft.
5. Theme Builder (footer/header/404/search-results/single-post/product/popup) → \`elementorTemplate_create\`. Only footer/header default their \`conditions\` to site-wide; every other type needs an explicit condition — ask if unclear rather than guessing. Anything else in Elementor's Theme Builder catalogue (Loop Item, Cookie Consent, ...) has no tool yet — say so, don't approximate it by editing a single page.
5a. Kit baseline — check once per site, before the first \`page_create\`/\`page_createFromHtml\`/\`page_addElementorWidget\`/\`elementorTemplate_create\` call on it in this conversation: call \`elementorKit_getLayout\` once. If \`container_width\` comes back unset, this site's Kit was never customized — apply the baseline before building anything (if it's already set, from any earlier task, skip straight to building — this only ever needs doing once per site, real persisted site state, not session state). Baseline: (1) always, regardless of whether a design was given — \`elementorKit_updateLayout\` with \`containerWidth: 1300\` and all six \`activeBreakpoints\` enabled (basic setup, not design-specific); (2) only if the request includes or implies a brand/design spec (palette, typography, mockup, screenshot) — set up Global Colors/Fonts via \`elementorKit_*\` (required when a spec exists, never as a Code Snippet/custom CSS): rename the 4 system color/font slots to the design's own names via \`elementorKit_updateColor\`/\`updateFont\`'s \`title\` (renaming a system slot is supported, only its \`_id\` is fixed), set their values, then \`elementorKit_addColor\`/\`addFont\` for every other palette entry (always include plain white/black among custom colors unless the design already has equivalents), with a fallback font family (\`"Arial"\` if unspecified). A referenced \`typography_font_family\` only renders if that font is actually available (system/Google Font by name, or a proprietary font uploaded via \`customFont_upload\` first — see rule 5d). No brand/design spec yet → skip step 2 silently, don't interrupt a plain content request to ask for a palette that was never part of it.
5b. Building a whole site from a design: after the 5a baseline check, break it into separate calls — never fold the footer/nav into a page's own content. Build each page with \`page_createFromHtml\`. Build the footer/header as a real Theme Builder template via \`elementorTemplate_create\` (see rule 5) — a footer added to a page's content only shows on that page. Build the navigation with \`menu_create\`/\`menuItem_create\` (see rule 5c). Report each piece's ID separately rather than one blanket "site built" summary.
5c. Navigation menus: \`menu_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` for the menu itself, \`menuItem_list\`/\`_create\`/\`_update\`/\`_delete\` for its items. A menu item is either a custom link (set \`url\`) or a link to an existing page/post/category (set \`objectType\` + \`objectId\`, WordPress derives the URL itself) — don't set both. \`parentId\` nests an item as a submenu entry. There's no API for assigning a menu to a theme location (header/footer nav) — tell the user that still needs doing manually in WP-admin → Appearance → Menus after building it.
5d. Custom/proprietary fonts: \`customFont_upload(filePath, family, weight?, style?)\` uploads a font file and registers a real \`@font-face\` for it (frontend + Elementor editor preview) — not via Elementor Pro's own closed-source Custom Fonts screen, but functionally equivalent once uploaded: reference the same \`family\` in \`elementorKit_addFont\`/\`updateFont\`'s \`typography_font_family\` and it renders. It won't show in Elementor's own Custom Fonts picker UI, only in the rendered result — that's cosmetic only. One upload per weight/style combination. \`customFont_list\`/\`customFont_delete\` to manage. Prefer a matching Google Font (no upload needed) unless the design genuinely requires a proprietary file.
6. For a brand-new Fluent Forms form use \`form_createFluentForm\` (read the \`fluent-forms\` resource for the field shape) rather than guessing a widget/field type. Third-party plugin widgets in general (Smart Slider 3, JetSmartFilters, ...) have no verified schema — don't guess a \`widgetType\`; check for a plain shortcode widget first, otherwise leave an explicit placeholder and ask the user how they'd like to proceed.
7. Multilingual sites (Polylang or WPML): \`site_healthCheck\`'s result includes a \`languages\` field (null on a single-language site, otherwise \`{ plugin, default, available }\`). When it's present and the user asks to create or update content without saying which language it's for, ask before calling \`page_create\`/\`page_createFromHtml\`/\`page_update\` — don't default silently to the site's default language. Pass the answer as \`lang\`. If the new content is a translation of an existing page, also ask for that page's ID and pass it as \`translationOfId\` to link them as a translation group.
8. For custom PHP/CSS/JS/HTML snippets (not Elementor content) on a site running the Code Snippets plugin (codesnippets.pro), use \`codeSnippet_create\`/\`codeSnippet_update\`/\`codeSnippet_list\`/\`codeSnippet_get\`/\`codeSnippet_delete\`/\`codeSnippet_activate\`/\`codeSnippet_deactivate\` instead of trying to add a snippet through any page tool. A new snippet is created inactive unless \`active: true\` is passed, and PHP snippets with a syntax error are forced back to inactive by the plugin itself — check the returned \`codeError\` field and tell the user if it's set rather than reporting success.
9. SEO metadata (title, meta description, focus keyword, canonical, social previews, noindex/nofollow) is per-plugin: \`yoast_get\`/\`yoast_update\`, \`rankmath_get\`/\`rankmath_update\`, \`aioseo_get\`/\`aioseo_update\`, \`seopress_get\`/\`seopress_update\`. Check \`/system\`'s \`adapters\` list (via \`site_healthCheck\`) for which one is actually active on a site before calling any of them — calling the wrong plugin's tool just 424s.
10. WooCommerce: \`woocommerceProduct_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` — simple products only, variable products (with variations) aren't supported yet.
11. Custom field plugins: \`acf_getAll\`/\`acf_get\`/\`acf_update\`/\`acf_updateAll\` (ACF), \`pods_get\`/\`pods_update\`/\`pods_updateAll\` (Pods), \`metaBox_get\`/\`metaBox_update\`/\`metaBox_updateAll\` (Meta Box) — \`selector\` is the field name/key, check \`/system\`'s \`adapters\` list for which plugin is actually active first. Pods and Meta Box have no "get all fields" tool (ask the user for the specific field name/key rather than guessing one).
12. Form plugins with their own dedicated tools (not Elementor widgets): \`cf7_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` (Contact Form 7), \`wpforms_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` (WPForms), \`ninjaForms_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` (Ninja Forms), \`formidable_list\`/\`_get\`/\`_create\`/\`_update\`/\`_delete\` (Formidable Forms). Check \`/system\`'s \`adapters\` list (via \`site_healthCheck\`) for which one is active first. Each plugin's \`fields\`/\`settings\`/\`mail\`/\`options\` are passed through in that plugin's own shape — don't invent a generic form-field schema across them. \`ninjaForms_update\` only changes title/settings, not fields/actions. \`formidable_delete\` trashes (recoverable) unless \`permanent\` is set.
13. End with a short, concrete confirmation: which site, which page (title + slug/URL), published or still a draft.`;
//# sourceMappingURL=instructions.js.map